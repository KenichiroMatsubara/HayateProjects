/**
 * Thin re-export shim for backwards compatibility.
 * All implementation lives in {@link ./style-codec.js}.
 */
export { encodeStylePatch, unsetKindsOf, TAG, UNSET_KIND } from './style-codec.js';
