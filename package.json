{
  "name": "hayate-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build:hayate": "cd Hayate && bash scripts/build-wasm.sh",
    "build:tsubame": "cd Tsubame && pnpm build",
    "build": "pnpm run build:hayate && pnpm run build:tsubame"
  },
  "packageManager": "pnpm@9.0.0"
}
