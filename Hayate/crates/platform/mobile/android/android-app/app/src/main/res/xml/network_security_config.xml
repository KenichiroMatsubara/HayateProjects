<?xml version="1.0" encoding="utf-8"?>
<!-- cleartext（平文 http）は LAN dev 用途に限定し、既定は https（ADR-0002・#740）。

     公開 Demo Endpoint（ADR-0003）は HTTPS で、OS スタック（HttpURLConnection）が TLS を担う。
     平文を許すのは開発機の dev-server に繋ぐローカル開発だけ：エミュレータから見たホスト機
     （10.0.2.2）と端末 loopback。networkSecurityConfig は IP レンジ（192.168.0.0/16 等）を
     表現できないため、実機から LAN の dev-server へ平文で繋ぐ場合は https を用意するか
     debug ビルドを使う。 -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android エミュレータからホスト機の loopback へ抜ける別名アドレス（既定 target）。 -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
