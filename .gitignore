# Archived git metadata from repo merge
Tsubame/.git-merged/

# Dependencies
node_modules/
.claude/worktrees/

# Build outputs (tsup / vite). Rebuilt from source; the release CI builds them
# before packing, so they're never committed (npm pack reads the working tree).
dist/
dist-torimi/
dist-android/
*.tsbuildinfo

# Cloudflare Workers local dev state
.wrangler/

# JetBrains IDE (Android Studio / IntelliJ) — 開発者ごとのプロジェクト設定。
# 開くたびに再生成・更新されるため追跡しない。
.idea/

# Playwright (E2E artifacts)
test-results/
playwright-report/
blob-report/
.playwright/

# Dev-only live taste-constant override (#353). The .example template is
# committed; the live file is a local scratchpad and never shipped.
Tsubame/examples/todo/public/tuning.jsonc
